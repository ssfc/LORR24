## Аннотация 摘要

翻译如下：

---

多智能体路径规划问题（MAPF）是指让多个智能体从起始位置移动到终点目标位置，同时避免相互碰撞的问题。长期多智能体路径规划（LMAPF）是MAPF问题的一种变体，在该问题中，智能体会不断被分配新的目标。本项目提出了解决LMAPF问题的方法。同时，作为一个附加目标，该方法还应用于2024年机器人竞速联盟（The League of Robot Runners 2024）比赛，这也为我们带来了一些有趣的研究问题及未来的发展方向。

本项目描述了三个主要的研究任务。第一个任务是在有限的规划时间（每步1秒）内，为大量智能体（约1万个）或极高密度的智能体（接近100%占用率）寻找高质量的LMAPF解决方案。第二个任务是减少拥堵现象以及减弱LMAPF算法中短视行为的影响。为了解决这些问题，项目提出了如设计“行驶指引”与“交通规则”来缓解拥堵，以及引入对未来的预测和实时搜索等方法。第三个任务是弥补文献中使用的LMAPF模型与实际应用之间的差距。

## 1 Введение 前言

### 1.1 Описание предметной области 问题领域说明

翻译如下：

---

多智能体路径规划问题（MAPF）是指在多智能体规划中，为多个智能体分别制定一组互不冲突的轨迹，每个智能体都有自己的起始位置和目标位置。智能体所活动的空间通过图结构进行建模，图中的节点表示智能体可能的位置，边则表示可选择的动作集合。路径规划时必须考虑时间因素，因为需要避免多个智能体同时占据同一个图节点或同一条边的冲突情况。

多智能体路径规划问题的最优解属于NP-完全问题，也就是说，在多项式时间内无法找到最优解。然而，仍存在一些算法能够在多项式时间内非常快速地找到解，但这些解的质量往往很低。这类算法通常采用启发式方法和规则系统来消除智能体之间的冲突。在本项目中，将提出一种类似的算法，力求在某些特定类型的问题中，其解的质量能够超越现有方法。

### 1.2 Постановка задачи 问题描述



### 1.3 Соревнование 竞赛



## 2 Обзор литературы 文献综述

### 2.1 Priority Inheritance with Backtracking



### 2.2 Windowed Parallel PIBT-LNS



### 2.3 Graph Guidance



### 2.4 Lazy constraints addition search for MAPF



### 2.5 Вывод обзора литературы  文献综述结论

PIBT 的不足之处在于，该算法最初是为不包含转弯的 MAPF 问题设计的，也就是说，PIBT 并未考虑**转弯**，这导致其在带有转弯版本的问题中，解的质量较差。LaCAM 能够给出高质量的结果，但由于状态树极其庞大，当代理数量很大时，计算速度很慢，而且本身并不适用于 lifelong 版本的任务，尽管可以限制其规划深度。WPPL 虽然能够进行一定深度的规划，从而减轻近视性问题的影响，但随着规划深度的增加，计算时间也会大幅增加。鉴于以上所有问题，本项目计划开发一种属于自己的方法，使其能够适用于大量代理、更少受近视性影响，并能产生更高质量的结果。

## 3 Описание предлагаемого метода 所提方法的描述

### 3.1 Priority Inheritance with Backtracking

PIBT 算法的原理在于为每个代理分配其期望的移动方向：左、右、上或下。最初，我们对所有代理进行一定的排序，例如按照到目标任务的估算距离进行排序。随后，我们按照这个顺序依次处理每个代理，并对每个代理进行递归构建。

假设我们对代理 r 调用了递归。我们希望为它选择一个移动方向。为此，首先为每个可能的移动方向分配一个优先级，比如执行该移动后到任务点的距离。然后按照这些优先级对方向进行排序，依次尝试。

我们将某一方向 d 分配给代理。如果该方向上没有其他代理占据，则我们成功地为路径上的所有代理进行了构建，可以退出递归。否则，如果该方向被其他尚未完成构建的代理占据，则通过递归调用尝试为其构建路径。如果构建失败，则继续尝试下一个方向。如果递归构建成功，则同样可以退出递归。

该算法存在一个较大的缺陷：它最初是为没有转弯的代理模型设计的。也就是说，代理每一步仅可向左、右、上、下移动。而我们的问题模型更加复杂，因为我们的代理移动过程中存在转弯。

### 3.2 Enhanced Priority Inheritance with Backtracking

在本工作中，提出了对 PIBT 算法的修改，即 EPIBT。与原始方法不同，EPIBT 考虑的不是单个动作，而是一系列动作的序列。这样可以解决 PIBT 中的问题，使其能够考虑到代理的转弯动作，同时通过操作的深度生成更高质量的路径。

具体操作的编码如下：W 表示等待，F 表示沿当前朝向前进，C 表示顺时针转向，R 表示逆时针转向。我们固定操作序列的深度，在我的算法中取 5，下面以 3 为例。那么我们会创建一个操作池，如 WWW、CWF、RWF、WWF、FRF、FCF、FFW、FFF 等等。这里列举了所有可能的动作组合，并按操作的“有用程度”进行排序：WWW 是最没用的（只是待在原地不动），FFF 是最有用的（连续三步向前，最好是朝向目标）。

同时，为每个操作 op 分配一个优先级 βₒₚ。在我的实现中，βₒₚ 就是该操作在操作池中的序号，例如 β(WWF) = 4。假设我们有代理 r，已知其在地图上的位置和目标点。我们要为操作 op 定义一个操作评价指标 wₒₚ，计算方式为：wₒₚ = (prev − cur) × α + βₒₚ，其中 prev 是代理 r 当前到目标的距离，cur 是代理 r 执行 op 后到目标的距离，α 是一个系数（我的实现中为 50）。

于是，在 EPIBT 中，一开始所有代理的操作都是 WWW，即都原地等待。接下来步骤与 PIBT 类似：按优先级排序后依次处理每个代理。选中代理 r，需要为其分配操作，递归调用代理的构建函数。将操作池按 wₒₚ 排序，依次尝试操作。取出某个操作 op，检查该操作形成的路径。如果该路径上没有其他代理（无碰撞），则将该操作分配给代理 r，递归返回，构建成功。如果路径上和其他代理有冲突，且涉及两个及以上代理，则跳过这个操作（否则递归复杂度过大）。如果只与一个代理 t 有冲突，则清除 t 的路径，把当前代理 r 分配该操作 op，然后对代理 t 递归地重新分配操作。

### 3.3 Enhanced Priority Inheritance with Backtracking Large Neighborhood Search
此外，本文还提出了对 EPIBT 的一种改进方案，用以解决 PIBT 算法的主要缺陷：优先级问题。在这类算法中，优先级会直接影响解的质量。

EPIBT+LNS 为所有代理的状态定义了一种度量方式。在我的实现中，这个度量是 \(\sum_{r} w_{op_r}\)，其中 \(op_r\) 表示代理 \(r\) 所选择的操作。该值越大，说明代理越能有效地向目标移动。

一开始，先执行 EPIBT 算法。随后，为了优化这一度量值，应用 LNS（大规模邻域搜索，采用模拟退火算法）。在 EPIBT+LNS 的每一步中，我们随机选择一个代理，并尝试用类似 EPIBT 的方法对其进行递归式重建。